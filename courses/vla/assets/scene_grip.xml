<mujoco model="scene">
    <!--
        scene_grip.xml — SO-101 scene modified for sim data collection.

        Changes from the standard mujoco_menagerie scene_box.xml:
          - Box placed at a reachable position (0.219, 0.024, 0.020) within arm workspace
          - Box mass increased for stability during data collection

        Usage: place this file next to so101.xml in your mujoco_menagerie checkout,
        then load with mujoco.MjModel.from_xml_path("scene_grip.xml").
        collect_demos.py does this automatically.
    -->
    <include file="so101.xml" />

    <visual>
        <headlight diffuse="0.6 0.6 0.6" ambient="0.3 0.3 0.3" specular="0 0 0" />
        <rgba haze="0.15 0.25 0.35 1" />
        <global azimuth="160" elevation="-20" />
    </visual>

    <asset>
        <texture type="skybox" builtin="gradient" rgb1="0.3 0.5 0.7" rgb2="0 0 0" width="512"
            height="3072" />
        <texture type="2d" name="groundplane" builtin="checker" mark="edge" rgb1="0.2 0.3 0.4"
            rgb2="0.1 0.2 0.3"
            markrgb="0.8 0.8 0.8" width="300" height="300" />
        <material name="groundplane" texture="groundplane" texuniform="true" texrepeat="5 5"
            reflectance="0.2" />
    </asset>

    <worldbody>
        <light pos="0 0 3.5" dir="0 0 -1" directional="true" />
        <geom name="floor" size="0 0 0.05" pos="0 0 0" type="plane" material="groundplane" />
        <!-- Box placed within arm reach at (0.219, 0.024) — matches scripted grasp pose -->
        <body name="box" pos="0.219 0.024 0.020">
          <freejoint/>
          <geom type="box" name="box" size="0.02 0.02 0.03" condim="3"
           friction="1 .03 .003" rgba="0 1 0 1" contype="2" conaffinity="1" solref="0.01 1"/>
        </body>
    </worldbody>

</mujoco>
